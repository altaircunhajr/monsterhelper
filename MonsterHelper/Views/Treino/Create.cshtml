@model MonsterHelper.Models.TreinoViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Treino</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Treino.Nome, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Treino.Nome)
                @Html.ValidationMessageFor(model => model.Treino.Nome)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Semana, new { @class = "control-label col-md-2" })
            <div class="col-md-10">

            @Html.CheckBoxListFor(x => x.Semana,
                                  x => x.Semana,
                                  x => x.Value,
                                  x => x.Text,
                                  x => )

            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jquferyval")
}
